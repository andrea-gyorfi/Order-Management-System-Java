# Order-Management-System-Java
Java application for managing client orders using a layered architecture (BLL, DAO, Model, GUI). Supports CRUD operations for clients, products, and orders, integrates a MySQL database, and uses reflection for dynamic query generation and table rendering.
